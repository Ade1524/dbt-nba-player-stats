version: 2

models:

  - name: rpt__regular_rookie_playoffs_mvps_full_players_stats_seasons_97_22
    description: This is a report model where we joined the fact model and the dimension models.
    columns:
      - name: dim_player_key
        description: This is the unique id of each player.
        tests:
            - unique
            - not_null

      - name: dim_season_key
        description: This is the unique id of each season.
        tests:
            - unique
            - not_null  

      - name: dim_team_key
        description: This is the unique id of each team.
        tests:
            - unique
            - not_null          